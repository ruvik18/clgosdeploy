kind = "web"
previewPath = "/"
title = "CounselIQ"
version = "1.0.0"
id = "artifacts/counselling"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25965

[services.development]
run = "pnpm --filter @workspace/counselling run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/counselling", "run", "build" ]
publicDir = "artifacts/counselling/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25965"
BASE_PATH = "/"
